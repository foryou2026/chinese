# D01-01 · AI 输出：数据规范

> **阶段**：D01 · 数据建模
> **归属**：**共享（_shared）**——跨系统共用，被各系统 D02 引用
> **角色**：数据建模师
> **上游（只读）**：B01(DB规范) + C01(共享需求) + C02(各系统交互) + C03(各系统原型)
> **落盘**：`docs/_shared/D01-data/<module-id>/<feature-id>/data-spec.md`

---

## 触发提示词

```
请你扮演"数据建模师"。
上游（只读已冻结）：C01 共享需求、C02 各系统交互规范、C03 各系统原型、B01 DB 规范。
模块 ID：<module-id>，功能 ID：<feature-id>，关联 R-ID：<...>
按 /prompt/D-develop/_shared/D01-01-AI输出-数据规范.md 输出。
落盘 docs/_shared/D01-data/<module-id>/<feature-id>/data-spec.md。
数据规范跨系统共享，需综合各系统 C02/C03 产出。
```

---

## AI 行为约束

1. **不重定义状态机**：状态枚举来自 C02，本阶段只建枚举列+DB 约束
2. **不写路由/接口/HTML**
3. **命名遵守 B01**
4. **不重定义 B01 全局表**：仅在关系中引用
5. **枚举与 C02 SM 对齐**：不一致必须声明
6. 未决项写末尾待确认

---

## 输出结构（单文件）

### 1. 概述

```markdown
# 数据规范 · <feature-id>

> **归属**：共享（_shared）
> **关联 R-ID**：R-XXX
> **不做**：状态机定义（在 C02）、路由/接口（在 D02）、页面/原型（在 C03）
```

### 2. ER 图

```mermaid
erDiagram
  COURSE ||--o{ CHAPTER : contains
```

### 3. 实体/表定义

每个实体包含：

**概述**

| 项 | 值 |
|----|-----|
| 关联 R-ID | |
| 业务定义 | |
| 状态机 | SM-XX / 无 |

**字段表**

| 字段 | 类型 | 必填 | 默认值 | 唯一 | 索引 | 说明 | 校验规则 |
|------|------|------|--------|------|------|------|---------|

**关系**

| 关系 | 目标表 | 基数 | 外键字段 | 删除策略 |
|------|--------|------|---------|---------|

### 4. 枚举定义

> 与 C02 SM 对齐。不一致写待确认。

| 值 | 中文名 | 说明 | 是否默认 |

### 5. 业务规则与校验

#### 5.1 业务约束

| BR-ID | 来源 R-ID | 涉及实体/字段 | 描述 | 实现层 |

> 实现层：DB constraint / DB trigger / Service / Application

#### 5.2 字段校验

| 实体 | 字段 | 校验规则 | 来源 R-ID | 实现层 |

#### 5.3 跨字段/跨表校验

### 6. 计算/派生字段

| 实体 | 字段 | 计算公式 | 实现方式 | 更新触发 |

> 实现方式：generated column / view / 应用层缓存 / cron

### 7. 索引策略

| IDX-ID | 表 | 字段 | 类型 | 唯一 | 支撑查询 |

### 8. 种子数据（无则写"无"）

| 表 | 用途 | 示例 | 写入时机 |

### 9. 待确认问题

| 编号 | 问题 | AI 默认方案 | 影响 |

### 10. AI 自检

**产出完整性**
- [ ] 所有实体有完整字段表
- [ ] 所有枚举列出完整值与默认值
- [ ] 所有外键标注删除策略
- [ ] ER 图与字段表一一对应

**上游一致性**
- [ ] 枚举值与 C02 SM 一致，未增删改名
- [ ] 未重定义 B01 全局表
- [ ] 命名遵循 B01
- [ ] 所有 R-ID 被实体或规则覆盖

**建模覆盖**
- [ ] 主键策略统一
- [ ] 关系完整（一对多/多对多/自关联）
- [ ] 唯一约束已覆盖
- [ ] 计算字段已决定存 vs 算
- [ ] 索引覆盖主要查询
- [ ] 软删除策略明确

**边界**
- [ ] 未画状态图、未输出路由/接口/HTML
- [ ] 单文件 <= 1200 行
