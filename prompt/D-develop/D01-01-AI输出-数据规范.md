# D01-01 AI 输出：数据规范

> **阶段**：D01·D 数据建模（共享）
> **上游**：B01(DB规范) + 各系统 C01/C02/C03
> **落盘**：`docs/D01-data/common/<module-id>/<feature-id>/`（多文件目录）

---

## 触发提示词

```
扮演"数据建模师"。
上游（已冻结）：各系统 C01 需求、C02 交互、C03 原型、B01 DB 规范。
模块：<module-id>，功能：<feature-id>，关联 R-ID：<...>
按 /prompt/D-develop/D01-01-AI输出-数据规范.md 输出。
落盘 docs/D01-data/common/<module-id>/<feature-id>/ 目录。
数据跨系统共享，需综合各系统 C02/C03 产出。
```

---

## AI 行为约束

1. 状态枚举来自 C02，只建枚举列+DB 约束，不重定义状态机
2. 不写路由/接口/HTML
3. 命名遵守 B01
4. 不重定义 B01 全局表，仅引用
5. 枚举与 C02 SM 不一致时必须声明
6. 未决项写 §99
7. **精简原则**：字段说明≤10 字、校验规则一句封顶、空节写"无"、禁止重复上游已有信息
8. 迁移文件落盘于 `system/` 下对应后端工程目录，禁止放在项目根目录
9. **生成后强制自检**：完成全部输出后，必须逐条对照 C01 需求清单（每条 R-ID）和 C02 状态机（每条转移），确认每条 R-ID 均有实体或规则承接、枚举值与 SM 一致。发现不一致或遗漏必须在 §99 声明

---

## 输出结构（多文件）

### 目录结构

```
docs/D01-data/common/<module-id>/<feature-id>/
├── 00-index.md               # 总览：ER图 + 业务规则 + 索引 + 种子数据 + 增量融合 + 自检
├── 01-<entity-name>.md       # 实体定义（每张表一个文件）
├── 02-<entity-name>.md
├── ...
└── enums.md                  # 所有枚举定义
```

> 实体文件按业务重要性排序编号。每文件独立可审阅。

---

### 00-index.md 结构

```markdown
# 数据规范 · <feature-id> · 总览

> **归属**：共享
> **关联 R-ID**：R-XXX
> **不做**：状态机定义(C02)、路由/接口(D02)、页面(C03)
> **实体清单**：见 01-xx.md ~ NN-xx.md
> **枚举清单**：见 enums.md

## 1. ER 图

```mermaid
erDiagram
```

## 2. 业务规则与校验

| BR-ID | 来源 R-ID | 涉及实体/字段 | 描述(一句) | 实现层 |

> 实现层：DB constraint / DB trigger / Service / Application

## 3. 索引策略

| IDX-ID | 表 | 字段 | 类型 | 唯一 | 支撑查询 |

## 4. 种子数据（无则写"无"）

| 表 | 用途 | 示例 | 写入时机 |

## 5. 增量融合报告
### 5.1 本轮新增摘要
### 5.2 融合点 / 冲突点 / 已有变更

## 6. 自检报告

> 生成后必须执行，逐项打勾。

**完整性**
- [ ] 每条 C01 R-ID 均有实体或规则承接？（逐条列出对应关系）
- [ ] 枚举值与 C02 SM 状态一致？
- [ ] 外键均标注删除策略？ER 图与字段表一一对应？

**命名与规范**
- [ ] 命名遵循 B01？未重定义 B01 全局表？
- [ ] 主键策略统一(uuid)？软删除策略明确？

**建模完备**
- [ ] 索引覆盖主查询路径？
- [ ] 计算字段已决定存 vs 算？

**边界**
- [ ] 未输出路由/接口/HTML/状态图？
- [ ] 无重复上游信息？

## 99. 待确认问题（无则写"无"）
| 编号 | 问题 | AI 默认方案 | 影响 |
```

---

### 实体文件结构（01-xx.md ~ NN-xx.md）

```markdown
# <表名>

| 项 | 值 |
|----|-----|
| 关联 R-ID | |
| 业务定义 | ≤15字 |
| 状态机 | SM-XXX / 无 |

## 字段

| 字段 | 类型 | 必填 | 默认值 | 唯一 | 说明(≤10字) | 校验(一句) |
|------|------|------|--------|------|-------------|-----------|

> B01 通用字段(id/created_at/updated_at)不再重复列出，除非默认值或约束有差异。

## 关系

| 目标表 | 基数 | 外键字段 | 删除策略 |

## 备注（可选，仅写非显而易见的设计决策）
```

---

### enums.md 结构

```markdown
# 枚举定义

> 与 C02 SM 对齐。不一致写 00-index.md §99。

## <枚举名>

| 值 | 中文名 | 默认 |
```
