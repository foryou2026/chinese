# 27 · H02 AI 澄清：原型提问清单生成模板

> **阶段**：H HTML 原型
> **谁产出**：AI（HTML 原型工程师）
> **落盘**：`docs/A00-meta/questions/H-<feature-id>-questions-round<N>.md`

---

## AI 必须遵守

1. **只读**：H 用户输入 + 本 feature C01-R 基线 + docs/C02-permissions/ + 本 feature C03-IA 全部 + 本 feature C04-N 全部页面规范 + docs/B02-ux/ + docs/B03-design/design-system/ + docs/B03-design/prototype-style/ + `A00-01` + `A00-03` + `A00-04` + 本模板。
2. **C/D 隔离铁律**：本阶段是产品设计阶段，**严禁**触碰任何后端实现细节——不读 docs/B01-architecture/、不假设接口签名、不假设数据库字段、不假设路由路径，也不向用户提问"这个调什么接口 / 字段叫什么 / URL 怎么写"。这些都是后续开发阶段的事，本阶段产出物里也不得出现。
3. 第一轮回复必为本模板实例化。
4. 阻断级 ≤ 8、优化级 ≤ 15。
5. 任何与 B02 决策、B03 token 冲突的备选不得出现。
6. **禁止**向用户提“是否分期 / 是否先做一部分 / 是否本期只做一部分 / 是否分优先级”这类问题——本阶段就是把 H01 §3 表里所有页面一次性出齐；如发现 H01 §3 与 C02 04-pages.md 不一致，按“是否一致”提问，不按“是否分期 / 优先级”提问。
7. 即使 H01 看似零歧义，也必须显式输出本轮自检结论；无问题时输出"本轮无澄清问题，进入 C05-H03"。

---

## 必盘点的 14 维

### A. 与上游一致性
1. H01 §1 / §3 列出的 page-id 是否与 C02 04-pages.md **完全一致**？多了 / 少了哪些？（多了→不在 IA 范围，应回 C02 处理；少了→询问是否漏写）
2. H01 §3 列出的 page-id 是否每一个都能在 docs/C04-pages/<feature-id>/ 找到对应 `<page-id>.md` + `<page-id>.scenarios.md`？缺哪些？
3. C02 03-state-machines.md 涉及的页面在原型里是否已要求表达不同状态下的差异（可见元素、可见操作、文案）？
4. B03 体验原则是否被违反（焦点态、动效时长、空状态文案语气、是否允许 emoji）？
5. B03 设计系统：本次需要的 token / 组件是否已存在？若不存在，是补 token（回 B03）还是绕开？**禁止**在 feature.css 重定义 token。
6. C02 角色清单与 H01 §1 主要角色是否一致？

### B. 原型实现边界
7. 数据来源：HTML 静态片段示例数据所用字段是否全部用业务语言（**禁用**数据库列名 / 后端字段名）？是否避免了独立 `mock-data.js` 文件？
8. 交互真实度：H01 §5 表单校验 / 列表分页 / 动效选择是否与 B03 一致？有冲突时以哪条为准？
9. 跳转：是否所有页面间跳转都用同目录 `./<page-id>.html` 相对锚？H01 是否出现真实 URL 路径需求（应拒绝并改为文件锚）？
10. 可访问性：键盘可达、`aria-*`、`prefers-reduced-motion` 是否本轮纳入？
11. 极简平铺：H01 §3 是否每个 page-id 只出 1 个默认态 HTML？是否确认不再生成 `*.empty.html` / `*.loading.html` / `*.error.html` / `*.forbidden.html`（这些状态由 docs/C06-prd/<feature>/ 文字承担）？
12. 移动端断点：375 / 768 / 1280 三档是否全做？
13. SEO / GEO：H01 §9 列出的公开页是否与 C03 各页 §11 一致？
14. 全局视觉诉求（H01 §6）若涉及 token 变更，是否已确认会回写 B03 源？

---

## 输出文件骨架

```markdown
<!-- TARGET-PATH: docs/A00-meta/questions/H-<feature-id>-questions-round<N>.md -->

# H 阶段澄清提问 · <feature-id> · 第 <N> 轮

> **上游**：H 用户输入（docs/C05-prototype/<feature-id>/_input/prototype-direction.md）+ 本 feature C01-R 基线 + 本 feature C03-IA 全部 + 本 feature C04-N 全部 + docs/C02-permissions/ + docs/B02-ux/ + docs/B03-design/design-system/ + docs/B03-design/prototype-style/
> **本阶段铁律**：不假设接口、不假设字段、不假设路由；不分期、不分批
> **本轮问题数**：阻断 X / 优化 Y

## A. 阻断级
### Q1. ...
- 背景（含上游引用：page-id / Block-ID / OP-ID / SM-ID / token 名）
- 候选 / 推荐 / 理由 / 影响范围

## B. 优化级
### Q9. ...

## C. AI 主动声明（无歧义但需告知用户的判断）
- D1. ...

## D. 决议（用户答完后回填到本文件此节）
```

> **回答完成后**：另存为 `H-<feature-id>-questions-round<N>-resolved.md` 发回 AI 触发 C05-H03 输出。

---

## 出题自检
- [ ] 14 维全过？
- [ ] 没出现"接口 / 字段 / 数据库 / endpoint / SQL / API-ID / URL 路径"等开发概念？
- [ ] 没出现“是否分期 / 本期是否只做一部分 / 二期再做”这类问题？
- [ ] 与 B02、B03 无冲突？
- [ ] H01 §3 的 page-id 全部能在 C03 阶段找到对应 `<page-id>.md`？
- [ ] 已确认每个 page-id 只出 1 个默认态 HTML、`<surface>/` 下无任何 `pages/` / `states/` / `assets/` 子目录？
- [ ] 单文件 ≤ 800 行？
