# C01-01 用户输入：需求与权限

> **阶段**：C01 需求与权限
> **归属**：**共享（_shared）**——跨系统共用，被各系统 C02 引用
> **谁来写**：你（产品/业务方）
> **落盘**：需求基线 → `docs/_shared/C01-requirements/<module-id>/<feature-id>/baseline.md`；权限变更 → `docs/_shared/C01-requirements/permissions/`
> **下一步**：贴给 AI，AI 按 C01-02 模板产出两份文件。

---

## 触发提示词

```
请你扮演"需求分析师 + 权限工程师"。
读取我下面贴的 C01 用户输入。
按 /prompt/C-product/_shared/C01-02-AI输出-需求与权限基线.md 格式产出两个文件：
1. 功能需求基线 → docs/_shared/C01-requirements/<module-id>/<feature-id>/baseline.md
2. 权限更新 → 增量更新 docs/_shared/C01-requirements/permissions/
不要输出流程图 / 状态机 / 页面布局——那是 C02 的职责。
每轮围绕一个功能，主动指出融合点和冲突点。
<贴下面填好的内容>
```

---

## 0. 元信息

| 字段 | 值 |
|------|----|
| 项目名称 | |
| 版本号 | |
| 填写人/日期 | |
| 模块 ID | `[kebab-case]` |
| 功能 ID | `[kebab-case]` |
| 涉及系统 | `[从 B01 08-systems.md 选]` |
| feature 类型 | `[ ] 单系统独占  [ ] 多系统共用` |
| 本轮聚焦功能 | |

---

## 一、需求部分

### 1. 一句话产品定义
> 给谁、解决什么问题、用什么方式

### 2. 业务背景（<= 200 字）

### 3. 目标用户（1-3 类）

#### 用户类型 1
- **画像**：
- **核心诉求**：
- **使用场景**：
- **当前替代方案及痛点**：

### 4. 核心业务目标（可验证）

- `[例：上线 3 月注册用户达 1 万]`
- `[例：D7 留存 >= 25%]`

### 5. 功能诉求

> 一行一条，每条：动作 + 对象 + 价值。

```
1. [例：用户能用邮箱注册并登录]
2. ...
```

### 6. 业务场景描述（一段话）

> 用户什么时候使用、成功后的价值。不画流程图、不拆步骤。

### 7. 业务规则与约束

- `[例：每用户每天最多 N 次操作]`

### 8. 输入/输出 & 第三方集成

- **数据输入来源**：
- **数据输出去向**：
- **第三方系统**：
- **需 mock 的**：

### 9. 风险与未知

### 10. 关联资料

---

## 二、权限部分

> AI 将把权限变更增量合并到 `docs/_shared/C01-requirements/permissions/` 下的共享文件。

### 1. 角色使用情况

> A. 沿用现有角色——列出哪些角色用本功能。B. 引入新角色——列 ID、职责、差异。

### 2. 角色 × 系统矩阵

| 角色 ID | `app` | `admin` | `[其他]` |
|---------|-------|---------|---------|

### 3. 多角色与角色组合

- 一用户能否多角色？`[ ] 能  [ ] 不能`
- 需要角色组/部门？`[ ] 需要  [ ] 不需要`
- 需要资源级权限？`[ ] 需要  [ ] 不需要`
- 跨系统共享账号？`[ ] 是  [ ] 各系统独立  [ ] 由 AI 推荐`

### 4. 权限粒度

- **粒度**：`[ ] 页面级  [ ] 功能点/按钮级  [ ] 字段级脱敏  [ ] 行级`
- **越权检查**：`[ ] 仅前端  [ ] 仅后端  [ ] 前+后  [ ] 含 DB RLS`

### 5. 数据可见范围

### 6. 审计日志

- 是否需要：`[ ] 必须  [ ] 可选  [ ] 不需要`
- 必记操作：
- 谁能看：
- 保留时长：

### 7. 合规要求

---

## 三、关联与融合

### 1. 与现有冻结模块的关联（必填，无则写"无"）

#### 1.1 依赖
| 现有 feature | 复用什么 | 来源文件 |
|------------|---------|---------|

#### 1.2 影响
| 受影响 feature | 影响点 | 需变更基线？ |
|--------------|-------|-----------|

#### 1.3 共享数据/状态机

#### 1.4 共享页面/入口

#### 1.5 显式不关联

### 2. 增量融合说明（非首轮时填）

---

## 四、补充

### 1. 想问 AI 的
### 2. 其他约束

---

## 填写自检

- [ ] 用户类型与角色对齐？
- [ ] 功能诉求有动作+对象+价值？
- [ ] 角色×系统矩阵与涉及系统一致？
- [ ] 关联部分已填写？
- [ ] 本轮聚焦功能明确？
