# 26 · H01 用户输入：原型方向（极简平铺）

> **阶段**：H HTML 原型
> **谁来写**：你（产品经理 / 交互设计师）
> **写完落盘**：`docs/C05-prototype/<feature-id>/_input/prototype-direction.md`
> **下一步**：贴给 AI（系统消息塞 `A00-01`、`A00-03`、`A00-04`、`C05-H02`、`C05-H03`、B02、B03、本 feature C01-R 基线、本 feature C03-IA 全部、本 feature C04-N 全部页面规范），AI 必须按 `C05-H02` 出澄清问题。

> **样式依赖**：HTML 仅通过相对路径引用 [`docs/B03-design/prototype-style/`](../../docs/B03-design/prototype-style/)（可含 feature 子目录），**绝不**引用 `/function/` 下任何资产、永不拷贝。

---

## 本阶段做什么、不做什么

| ✅ 本阶段产出（产品设计阶段）| ❌ 本阶段不做（开发件 / 后续阶段 / PRD 文字承担） |
|------------------------------|----------------------------------|
| 一整套零依赖、可双击运行的静态 HTML 原型 | 真实接口 / endpoint / SQL / 表名 / 列名 |
| **一个页面 = 一个默认态 HTML**，平铺在 `<surface>/` 根下 | 空 / 加载 / 错 / 无权限 等独立状态文件（这些由 docs/C06-prd/ 文字承担）|
| HTML 通过相对路径引用 `docs/B03-design/prototype-style/`（不拷贝）| 任何在 feature 目录下拷贝 B03 资产、任何 `vendor/` / `assets/` / `pages/` / `states/` 子目录 |
| 移动端 / 桌面端断点表现 | `mock-data.js` / `feature.css` / `feature.js`（示例数据直接写在 HTML 静态片段；微调内联） |
| 文案与 B02-ux/04-voice-tone.md 一致 | 框架 / 打包器 / npm 依赖 / 真实路由路径 / 真实第三方账号 |

---

## 触发提示词

```
请你扮演"HTML 原型工程师"，遵循 /prompt/A-framework/A00-01、/prompt/A-framework/A00-03、/prompt/A-framework/A00-04，硬性遵守 docs/C02-permissions/、docs/B02-ux/、docs/B03-design/design-system/、docs/B03-design/prototype-style/。
本阶段任务：把本 feature 已冻结的 C03 IA + C04 N 页面规范，落成一整套零依赖、双击 index.html 即可跑的静态 HTML 原型——**一个页面 = 一个默认态 HTML，平铺在 <surface>/ 下**，**不写**任何接口 / 路由 / 字段 / 数据库，**不画**任何 *.empty / *.loading / *.error / *.forbidden 状态文件。
feature-id=<>。
上游：本 feature C01-R 基线、本 feature C03-IA、本 feature C04-N 全部页面规范、B02、B03（已冻结）。
读取我下面贴的 H 用户输入。
本轮回复必须按 /prompt/C-product/C05-H02-AI澄清-原型提问.md 输出澄清清单，
落盘 docs/A00-meta/questions/H-<feature-id>-questions-round1.md。
本阶段是产品设计阶段，禁止涉及任何接口 / 路由 / 数据库 / 字段 / 后端实现。
<贴下面填好的内容>
```

---

## 1. feature 身份

> 本节由你从上游冻结产物里抄过来即可。

- **feature-id**：
- **feature 名称**：
- **所属 surface**（来自 B01 08-surfaces.md；双端 feature 列出 admin + app 两端，单端只列一端）：
- **关联 R-ID 范围**（来自 C01-R 基线 §4 需求清单）：
- **关联 page-id 列表**（来自 C02 04-pages.md，全量列出；按 surface 分组）：
  ```
  app:   P-app-xxx-001, P-app-xxx-002, ...
  admin: P-admin-xxx-001, P-admin-xxx-002, ...
  ```
- **关联 SM-ID 列表**（来自 C02 03-state-machines.md / `_shared/state-machines.md`；如无可留空）：
- **主要角色**（来自 C02 01-roles.md，且需在角色×surface 矩阵中此 surface 上 ✅）：
- **B02 / B03 是否已冻结**：`[ ] 是`
- **是否使用 feature 专属样式包**（路径：`docs/B03-design/prototype-style/<feature>/`）：`[ ] 是  [ ] 否`
- **本原型与现有 feature 的入口关联**（来自 C01-R01 §15.4，复述确认；无写"无"）：

---

## 2. 这个原型要解决什么

> 一句话。比"做出原型给评审看"更具体——"让评审在 5 分钟内点通'课程购买 → 学习 → 评价'主链路"。

```
```

---

## 3. 必出页面

> 把 §1 列出的 page-id 全部排进来。**不分批不分期**，本表里出现的就是本轮要全部出齐的范围；**每个 page-id 出 1 个默认态 HTML，平铺**。

| page-id | 所属 surface | 名称 | 备注（移动优先 / 公开页需 SEO+GEO / 是否暗黑 / 是否权限页）|
|---------|------|------|--------------------------------------------|
| P-app-xxx-001 | app | | |
| P-app-xxx-002 | app | | |
| P-admin-xxx-001 | admin | | |

> **空 / 加载 / 错 / 无权限态不在本表**——这些状态由 docs/C06-prd/<feature>/ 文字描述承担，不出图。
> **没有 P0/P1/P2 / 优先级 / 待定 / 二期 这种说法**——要么进表（都要做），要么不进表（不做）。

---

## 4. 公共骨架与导航

- 顶栏 / 侧栏 / 底栏在哪些页面出现：
- 三层 index.html（默认全要）：`[ ] C05-prototype/index.html  [ ] <feature>/index.html  [ ] <surface>/index.html`
- 主题切换器是否本轮上：`[ ] 上  [ ] 不上`（暗黑模式来自 B03，不在原型重写 token）

---

## 5. 交互真实度

| 维度 | 选项 | 你的选择 |
|------|------|---------|
| 表单校验 | 仅前端校验 / 校验 + 模拟 toast | |
| 列表分页 | 静态分页 / 仅显示首页一屏 | |
| 弹窗 / 抽屉 / 下拉 / 吐司 | 全部调 B03 prototype-style `proto.*` API（默认）| |
| 跳转 | 同目录 `<a href="./<page-id>.html">`（**禁用任何真实 URL 路径**） | |
| 数据来源 | **直接在 HTML 静态片段中写示例数据**（业务语言，禁用真实接口字段名）；**不**产出 `mock-data.js` | |
| 动效 | 与 B03 一致；`prefers-reduced-motion` 是否本轮纳入：`[ ] 是  [ ] 否` | |

---

## 6. 全局视觉诉求（可选，影响多页）

> 任何 token 调整必须直接改 `docs/B03-design/design-system/01-tokens.md` 与 `docs/B03-design/prototype-style/tokens.css`（本 feature 是**引用**，改后自动生效，无需重拷）。**禁止**直接在原型 HTML 内联样式里改 token。

- `[例：表格行高从 40 调到 36]`
- `[例：主色从 brand-500 改 brand-600]`

---

## 7. 响应式与可访问性

- 必须无破的断点：`[ ] 375  [ ] 768  [ ] 1280`（建议三档全选）
- 移动端关键改变（哪些块折叠 / 哪些 CTA 固定底部）：
- 键盘可达 / `:focus-visible` 焦点环 / `<label for>`：默认全部要求满足
- ARIA 角色与标签的特殊要求：

---

## 8. 文案

- 主标题 / 主 CTA / 错误兜底文案：与 `docs/B02-ux/04-voice-tone.md` 对照表一致（默认）
- 是否有 feature 专属术语需 AI 注意：

---

## 9. SEO / GEO（如适用，仅公开页）

> 公开页才填；非公开页留空。

- 公开页 page-id 列表：
- SEO meta（title / description / og:* / 结构化数据需求）：
- GEO 摘要段（≤ 150 字，便于 AI 抓取作答）：
- 禁止 AI 引用的内容：

---

## 10. 验收标准

> 在原型中能复现哪些场景才算本轮通过。直接抄本 feature 各页 `<page-id>.scenarios.md` 中的 SC-ID。

- 走通的关键场景：SC-1（来自 P-001）、SC-3（来自 P-002）……
- 移动端 375 下不出现横向滚动
- 无控制台报错
- 每个 page-id 都有对应的默认态 HTML（平铺在 `<surface>/` 根下）
- **无任何** `pages/` / `states/` / `assets/` / `vendor/` 子目录，**无任何** `*.empty.html` / `*.loading.html` / `*.error.html` / `*.forbidden.html`
- HTML 中全部 link / script 均通过相对路径引用 `docs/B03-design/prototype-style/`（可含 feature 子目录），feature 目录下**无任何**拷贝产物；无任何 `/function/` 路径引用
- 全文未出现任何接口名 / SQL / 表名 / 列名 / 真实路由路径

---

## 11. 不确定的事

```
```
